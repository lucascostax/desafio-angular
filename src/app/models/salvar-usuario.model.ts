export interface CriarUsuario {
    id: number;
    nome: string;
    email: string;
    telefone: string;
}

export interface CadastrarFilmes {
    id: number;
    titulo:string;
    genero:string;
}

export interface CriarGenero {
    id: number;
   nome:string;
}
